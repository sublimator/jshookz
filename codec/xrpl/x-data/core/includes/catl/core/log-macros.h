#pragma once
// Stub log macros for WASM — no-op everything
#define LOGD(...) do {} while(0)
#define LOGE(...) do {} while(0)
#define LOGW(...) do {} while(0)
#define LOGI(...) do {} while(0)
#define LOGD_KEY(label, key) do {} while(0)
#define OLOGD_KEY(label, key) do {} while(0)
