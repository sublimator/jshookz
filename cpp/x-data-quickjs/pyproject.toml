[project]
name = "jshookz-xdata-quickjs-tests"
version = "0.0.1"
requires-python = ">=3.11"
dependencies = [
    "hypothesis>=6.156.1",
    "pytest",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "property: bounded Hypothesis property tests for generated codec inputs",
]
