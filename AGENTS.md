See [.ai-docs/index.md](.ai-docs/index.md).

For local validation, use `scripts/run-tests.sh` with explicit pytest targets
or CTest regular expressions. The unscoped full gate is intentionally reserved
for `CI=1 scripts/run-tests.sh`.
